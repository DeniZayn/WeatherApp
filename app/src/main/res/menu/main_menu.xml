<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <item android:id="@+id/idHistory"
        android:title="History"
        android:icon="@android:drawable/ic_menu_recent_history"
        app:showAsAction="always"/>

    <item android:id="@+id/getContacts"
        android:title="Contacts"
        android:icon="@drawable/ic_baseline_contacts_24"
        app:showAsAction="always"/>

    <item android:id="@+id/getLocation"
        android:title="Location"
        android:icon="@drawable/ic_baseline_gps_fixed_24"
        app:showAsAction="always"/>

    <item android:id="@+id/showMaps"
        android:title="Location"
        android:icon="@android:drawable/ic_dialog_map"
        app:showAsAction="always"/>
</menu>